# python project cloner

receives git urls for simple python projects and clones those repos
orchestrates transparent debianization, cythonization, requirements.txt, setup.py, pyproject.toml, etc.
triggers the performance metric aware package builder
